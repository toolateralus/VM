// this class is mandatory. all calls to 
class server {
    constructor(id) {
        // this field is used for the gfxContext, but is also your process id.
        // this will likely be deprecated soon, but is neccesary for many graphics related things for now.
        this.id = id;
        }
}